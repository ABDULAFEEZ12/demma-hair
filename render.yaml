services:
  - type: web
    name: dimmahairshop
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 3 --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
